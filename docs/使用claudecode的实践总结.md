使用Claudecode时，不能太依赖它来自动编写代码，不能它会破坏原有的代码的结构和注释。

最佳实践，如使用它来完成温度传感器的功能时，不能这样做如帮我生成温度传感器的代码，如果这样做，它会修改原有的代码。
我们应该先创建一个文件夹让它来完成onewire的驱动代码，然后在另一个文件夹中让它来完成温度传感器的代码。这样约束它只能在指定的文件夹中生成代码，不会修改原有的代码，并且这样采用分层架构可以方便地维护和扩展。
这样做不仅方便维护，而且还能节省token的使用，因为它只会在指定的文件夹中生成代码，不会修改原有的代码。